# Base URL of the Spotlight backend (must match backend PORT)
REACT_APP_API_BASE_URL=http://localhost:3001

# No map API key needed - the map runs on MapLibre GL + OpenFreeMap
# (https://openfreemap.org), which is free, unlimited, and key-free.
