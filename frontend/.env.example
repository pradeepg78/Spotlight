# Base URL of the Spotlight backend (must match backend PORT)
REACT_APP_API_BASE_URL=http://localhost:3001

# Mapbox public token - https://account.mapbox.com/access-tokens/
REACT_APP_MAPBOX_TOKEN=your_mapbox_token
